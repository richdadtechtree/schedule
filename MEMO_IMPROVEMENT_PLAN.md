# 메모 기능 개선 계획서

**작성일:** 2026-05-18  
**대상:** schedule 앱 메모(Memo) 모듈  
**현황 요약:** 현재 메모는 제목·내용·색상·카테고리·체크리스트·잠금을 지원하는 기본형 노트 기능을 제공하며, 평문(Plain Text) 입력만 가능하고 정렬·필터·에디터 기능이 제한적임

---

## 1. 개선 방향 요약

| 구분 | 방향 |
|------|------|
| 에디터 | 평문 → 마크다운 지원으로 표현력 향상 |
| 구성 | 카테고리 단일 체계 → 태그 복합 체계로 유연한 분류 |
| 탐색 | 텍스트 검색 → 필터·정렬 옵션 추가 |
| 뷰 | 그리드 고정 → 그리드/리스트 전환 |
| 연계 | 메모 독립 → 일정·할일과 연결 |
| 미디어 | 텍스트 전용 → 이미지 첨부 지원 |
| 자동화 | 수동 입력 → 템플릿·AI 요약 |

---

## 2. 단계별 실행 계획

### Phase 1 — 핵심 UX 개선 (즉시 시작)

> 기존 데이터 구조를 크게 바꾸지 않고 체감 품질을 빠르게 올리는 작업

#### 1-1. 마크다운 렌더링 지원
- **문제:** 현재 메모 내용은 `white-space: pre-wrap`으로만 표시됨. 볼드, 링크, 목록 표현 불가
- **구현:**
  - Marked.js 또는 marked-lite 라이브러리 추가 (CDN, ~11KB gzip)
  - 메모 뷰 모달의 `.mv-content`에 `marked.parse(content)` 적용
  - 편집 시에는 원본 평문 유지, 보기 시에만 렌더링
  - 마크다운 단축키 툴바 (B / I / `코드` / - 목록) 편집 모달 상단에 추가
- **영향 범위:** `_renderMemoView()`, `.mv-content` CSS

#### 1-2. 정렬 옵션 추가
- **문제:** 메모가 생성 순(역순)으로만 나열됨. 최근 수정·이름순·색상별 정렬 불가
- **구현:**
  - 메모 그리드 상단에 정렬 드롭다운 추가
  - 정렬 기준: 최신순(기본) / 오래된순 / 제목 가나다순 / 색상별 / 수정일순
  - `memoSortOrder` 변수로 관리, `localStorage`에 유지
- **영향 범위:** `renderMemosView()` 내 필터 파이프라인

#### 1-3. 그리드 / 리스트 뷰 전환
- **문제:** 메모가 많아질수록 3열 그리드에서는 내용 파악이 어려움
- **구현:**
  - 메모 뷰 우상단에 뷰 전환 버튼 (격자 아이콘 / 리스트 아이콘)
  - 리스트 뷰: 1열, 카드 높이 64px, 제목+내용 한 줄 미리보기, 색상 좌측 바로 표시
  - `memoViewMode` 변수로 관리, `localStorage`에 유지
- **영향 범위:** `renderMemosView()`, CSS 추가

#### 1-4. 메모 카드 내용 미리보기 개선
- **문제:** 현재 카드 내용이 60자로 잘려 정보 밀도가 낮음
- **구현:**
  - 카드 최대 높이를 180px로 고정하고 `overflow: hidden` + 그라디언트 페이드로 자연스럽게 잘림
  - 마크다운 렌더링 적용 시 태그 제거(`strip tags`)한 순수 텍스트로 미리보기
  - 체크리스트 미리보기: 현재 4개 → 최대 5개, 완료 항목 취소선 표시

---

### Phase 2 — 구성 및 연결 강화 (1~2주)

#### 2-1. 태그 시스템 추가
- **문제:** 카테고리는 메모당 1개만 지정 가능. 여러 맥락에 속하는 메모를 분류하기 어려움
- **구현:**
  - 메모 데이터에 `tags: string[]` 필드 추가 (Supabase 컬럼 추가)
  - 편집 모달에 태그 입력 필드: `#태그명` 입력 → Enter로 추가, 뱃지로 표시, 클릭으로 제거
  - 카드 하단에 태그 뱃지 최대 3개 노출
  - 사이드바에 태그 필터 섹션 추가 (카테고리 아래)
  - 검색 시 `#태그명`으로 태그 검색 지원
- **영향 범위:** 데이터 구조, 편집/뷰 모달, 사이드바, 검색 로직

#### 2-2. 일정·할일 연계
- **문제:** 메모와 캘린더·태스크가 완전히 분리되어 있어 컨텍스트 연결이 없음
- **구현:**
  - 메모 뷰 모달 하단에 "연결된 일정" / "연결된 할일" 섹션 추가
  - 할일 생성 시 "메모에서 생성" 출처 표시
  - 메모 내에서 `[[태스크 제목]]` 문법으로 태스크 링크 생성
  - 할일·일정 카드에서 연결된 메모 뱃지 표시
- **영향 범위:** 태스크/이벤트 데이터 구조, 메모 뷰 모달

#### 2-3. 이미지 첨부
- **문제:** 텍스트 전용으로 레퍼런스 이미지나 스크린샷 보관 불가
- **구현:**
  - 편집 모달에 이미지 업로드 버튼 추가
  - Supabase Storage에 `memo-images/` 버킷 업로드
  - 이미지 URL을 `images: string[]` 필드로 저장
  - 메모 뷰 모달에 이미지 갤러리 표시 (가로 스크롤)
  - 카드에는 첫 번째 이미지 썸네일 표시 (우상단 작은 아이콘)
  - 붙여넣기(Ctrl+V)로 클립보드 이미지 직접 첨부 지원
- **영향 범위:** 데이터 구조, 편집/뷰 모달, Supabase Storage 설정

---

### Phase 3 — 생산성 도구 (2~4주)

#### 3-1. 메모 템플릿
- **문제:** 회의록·학습노트·아이디어 메모 등 반복되는 형식을 매번 처음부터 작성
- **구현:**
  - 기본 제공 템플릿 (5종):
    - 📋 회의록 (날짜, 참석자, 논의사항, 액션아이템)
    - 💡 아이디어 노트 (문제, 해결책, 다음 단계)
    - 📖 학습 노트 (핵심 개념, 예시, 복습 질문)
    - ✅ 프로젝트 계획 (목표, 체크리스트)
    - 📝 일기 (날씨, 오늘 한 일, 내일 할 일)
  - 새 메모 생성 시 템플릿 선택 화면 표시
  - 사용자 커스텀 템플릿 저장 기능 (`memo_templates` localStorage/Supabase)
- **영향 범위:** 새 메모 생성 플로우, 신규 템플릿 관리 모달

#### 3-2. 메모 내보내기
- **문제:** 메모를 외부로 꺼내거나 백업하는 수단 없음
- **구현:**
  - 메모 뷰 모달 우상단 내보내기 메뉴 추가
  - 형식: 마크다운(.md) / 평문(.txt) / JSON(백업)
  - 전체 메모 일괄 내보내기: ZIP으로 묶어 다운로드
  - 클립보드 복사 (기존 기능 유지)
- **영향 범위:** 메모 뷰 모달, 설정 화면

#### 3-3. 메모 검색 고도화
- **문제:** 현재 제목·내용 텍스트 매칭만 지원. 카테고리·색상·날짜 범위로 찾기 불가
- **구현:**
  - 검색창 아래 고급 필터 패널 (토글):
    - 카테고리 다중 선택
    - 태그 필터
    - 색상 필터 (색상 팔레트 클릭)
    - 날짜 범위 (생성일 / 수정일)
    - 잠금 상태 (잠금 / 미잠금)
    - 체크리스트 유무
  - 활성 필터 뱃지 표시 (검색창 아래)
  - 필터 저장 기능 ("자주 쓰는 검색" 북마크)
- **영향 범위:** 검색 로직, 필터 UI 추가

#### 3-4. 버전 히스토리 (자동 저장)
- **문제:** 메모 편집 후 이전 내용으로 되돌릴 방법 없음
- **구현:**
  - 저장 시 이전 버전을 `memo_history` 테이블에 기록 (최대 20개 유지)
  - 메모 뷰 모달에 "히스토리" 버튼 추가
  - 히스토리 패널: 버전 목록(날짜+시간), 선택 시 diff 미리보기, 복원 버튼
- **영향 범위:** Supabase 테이블 추가, 저장 로직, 신규 히스토리 UI

---

### Phase 4 — AI 기능 (4주+)

#### 4-1. AI 메모 요약
- **기능:** 긴 메모를 3~5문장으로 요약
- **구현:** Claude API 호출, 메모 뷰 모달 내 "AI 요약" 버튼
- **UI:** 요약 결과를 접을 수 있는 패널로 표시, 재생성 가능

#### 4-2. AI 자동 태그 추천
- **기능:** 메모 내용 기반으로 태그 자동 추천
- **구현:** 저장 시 백그라운드 Claude API 호출 → 추천 태그 3개 제시 → 사용자 승인
- **UI:** 편집 모달 저장 후 "이런 태그 어때요?" 토스트 알림

#### 4-3. AI 메모 → 할일 변환
- **기능:** 메모 내용에서 액션 아이템을 추출해 할일로 자동 생성
- **구현:** 메모 뷰 모달 내 "할일 추출" 버튼 → Claude가 추출한 항목 목록 표시 → 선택하여 태스크 생성

---

## 3. 우선순위 매트릭스

| 개선 항목 | 영향도 | 구현 난이도 | 우선순위 |
|-----------|--------|-------------|----------|
| 마크다운 렌더링 | ★★★★★ | 낮음 | **즉시** |
| 그리드/리스트 뷰 전환 | ★★★★☆ | 낮음 | **즉시** |
| 정렬 옵션 | ★★★★☆ | 낮음 | **즉시** |
| 카드 미리보기 개선 | ★★★☆☆ | 낮음 | **즉시** |
| 태그 시스템 | ★★★★★ | 중간 | Phase 2 |
| 검색 고도화 | ★★★★☆ | 중간 | Phase 2 |
| 메모 템플릿 | ★★★★☆ | 중간 | Phase 3 |
| 이미지 첨부 | ★★★★☆ | 높음 | Phase 2 |
| 일정·할일 연계 | ★★★☆☆ | 높음 | Phase 2 |
| 내보내기 | ★★★☆☆ | 낮음 | Phase 3 |
| 버전 히스토리 | ★★★☆☆ | 높음 | Phase 3 |
| AI 요약 | ★★★★☆ | 중간 | Phase 4 |
| AI 태그 추천 | ★★★☆☆ | 중간 | Phase 4 |
| AI 할일 변환 | ★★★★☆ | 중간 | Phase 4 |

---

## 4. 데이터 구조 변경 계획

### 현재 Memo 스키마
```json
{
  "id": "string",
  "title": "string",
  "content": "string",
  "color": "string",
  "category": "string",
  "checklist": "string",
  "locked": "boolean",
  "dirty": "boolean"
}
```

### 목표 Memo 스키마 (Phase 2 이후)
```json
{
  "id": "string",
  "title": "string",
  "content": "string",
  "content_format": "plain | markdown",
  "color": "string",
  "category": "string",
  "tags": ["string"],
  "checklist": "string",
  "images": ["string"],
  "linked_task_ids": ["string"],
  "linked_event_ids": ["string"],
  "locked": "boolean",
  "dirty": "boolean",
  "created_at": "timestamp",
  "updated_at": "timestamp"
}
```

---

## 5. 기술 스택 추가 사항

| 항목 | 라이브러리/방법 | 이유 |
|------|-----------------|------|
| 마크다운 파싱 | `marked` (CDN) | 경량, XSS sanitize 내장 |
| 이미지 업로드 | Supabase Storage | 기존 인프라 활용 |
| AI 기능 | Claude API (claude-sonnet-4-6) | 기존 환경 활용 |
| 내보내기 | `JSZip` (CDN) | 일괄 ZIP 내보내기 |

---

## 6. 마일스톤

```
Week 1     : Phase 1 완료 (마크다운, 정렬, 뷰 전환, 카드 개선)
Week 2~3   : Phase 2 완료 (태그, 이미지 첨부, 연계)
Week 4~5   : Phase 3 완료 (템플릿, 내보내기, 검색 고도화)
Week 6~7   : Phase 3 완료 (버전 히스토리)
Week 8+    : Phase 4 (AI 기능, 우선순위에 따라 순차 적용)
```
